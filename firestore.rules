rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Colección de miembros: lectura pública, escritura solo para admins autenticados
    match /miembros/{docId} {
      allow read: if true;
      allow write: if request.auth != null &&
                      exists(/databases/$(database)/documents/admind/$(request.auth.token.email));
    }

    // Colección de admins: solo lectura para usuarios autenticados
    match /admind/{email} {
      allow read: if request.auth != null;
      allow write: if false; // solo se gestiona desde Firebase Console
    }
  }
}
